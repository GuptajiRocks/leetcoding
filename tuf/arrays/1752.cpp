#include <iostream>
#include <vector>
using namespace std;

bool pups(vector<int> arr, int n) {
    bool curr;

    for (int i = 0; i < n; i++) {
        
    }
}

int main() {
    vector<int> arr = {3, 4, 5, 1, 2};
    int n = arr.size();

    bool res = pups(arr, n);

    cout << res << endl;

    return 0;
}